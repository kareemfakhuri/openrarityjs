[tool.poetry]
name = "openrarity-js"
version = "1.0.0"
description = ""
authors = ["kareemfakhuri"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10,<3.12"
open-rarity = "^0.7.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
